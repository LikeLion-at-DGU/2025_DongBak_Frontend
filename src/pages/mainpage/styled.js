import styled from "styled-components";

export const Wrapper = styled.div`
  width: 100%;
  max-width: 540px;
  height: 100vh; /* 화면 높이에 맞춰 변경 */
  background: url("/images/mainBackImage.svg") no-repeat center / cover;
  position: relative;
`;